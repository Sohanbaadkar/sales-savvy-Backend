version: "3.9"

services:
  
  # MySQL database
  db:
    image: mysql:8.0
    container_name: salessavvy-mysql
    environment:
      MYSQL_ROOT_PASSWORD: "sohan546"   # quotes needed for '@'
      MYSQL_DATABASE: sales_savvy_project
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -p\"${MYSQL_ROOT_PASSWORD}\" --silent"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Spring-Boot application
  app:
    build: .
    container_name: salessavvy-backend
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Override JDBC settings so the app talks to the *container* DB
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/sales_savvy_project
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: "sohan546"
      SPRING_JPA_HIBERNATE_DDL_AUTO: update

      # JWT & payment keys (use secrets in real prod)
      JWT_SECRET: your-very-long-secure-secret-key-for-production-12345678901234567890
      JWT_EXPIRATION: "3600000"
      RAZORPAY_KEY_ID: rzp_test_RICAxWpUhTcaF3
      RAZORPAY_KEY_SECRET: sn11wDgxWINKMl0MOQ084Bsa
    restart: unless-stopped

  # Frontend application
  frontend:
    build: C:\Users\sohan\OneDrive\Desktop\SalesSavvy\salessavvy
    container_name: salessavvy-frontend
    ports:
      - "5173:5173"
    depends_on:
      - app
    restart: unless-stopped

volumes:
  mysql_data:
